package com.example.rest_kafka_poc.service;import com.example.rest_kafka_poc.entity.EmployeeEntity;import com.example.rest_kafka_poc.pojo.Employee;import com.example.rest_kafka_poc.repo.EmployeeRepo;import com.google.gson.Gson;import lombok.extern.slf4j.Slf4j;import org.springframework.beans.factory.annotation.Autowired;import org.springframework.stereotype.Service;import org.springframework.util.ObjectUtils;@Slf4j@Servicepublic class EmployeeServiceImpl implements EmployeeService {    @Autowired    EmployeeKafkaImpl employeeKafka;    @Autowired    EmployeeRepo employeeRepo;    @Override    public void createEmployee(EmployeeEntity employeeEntity) {        employeeRepo.save(employeeEntity);        //log.info("Record stored in DB");    }    @Override    public String getEmployee(int id) {        EmployeeEntity employee = employeeRepo.getById(id);        return !ObjectUtils.isEmpty(employee) ? employee.toString() : null;    }    @Override    public String publishEmployee(Employee employee) {        employeeKafka.producer(employee.toString());        return employee.toString();    }}