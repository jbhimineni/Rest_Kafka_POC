package com.example.rest_kafka_poc.controller;import com.example.rest_kafka_poc.pojo.Employee;import com.example.rest_kafka_poc.service.EmployeeServiceImpl;import com.google.gson.Gson;import org.junit.jupiter.api.BeforeEach;import org.junit.jupiter.api.Test;import org.junit.jupiter.api.extension.ExtendWith;import org.mockito.InjectMocks;import org.mockito.Mock;import org.mockito.junit.jupiter.MockitoExtension;import org.springframework.http.MediaType;import org.springframework.test.web.servlet.MockMvc;import org.springframework.test.web.servlet.setup.MockMvcBuilders;import static org.mockito.Mockito.*;import static org.springframework.test.web.servlet.request.MockMvcRequestBuilders.*;import static org.springframework.test.web.servlet.result.MockMvcResultMatchers.*;@ExtendWith(MockitoExtension.class)class EmployeeController_Test {    @Mock    private EmployeeServiceImpl employeeServiceImpl;    @InjectMocks    private EmployeeController employeeController;    private MockMvc mockMvc;    @BeforeEach    void setUp() {        mockMvc = MockMvcBuilders.standaloneSetup(employeeController).build();    }    @Test    void testGetWish() throws Exception {        mockMvc.perform(get("/employee/"))                .andExpect(status().isOk())                .andExpect(content().string("Hello"));    }    @Test    void testPostEmployeeRecord() throws Exception {        Employee employee = getEmployeeRecord();        when(employeeServiceImpl.publishEmployee(any(Employee.class)))                .thenReturn(employee.toString());        mockMvc.perform(post("/employee/create")                        .contentType(MediaType.APPLICATION_JSON)                        .content(new Gson().toJson(employee)))                .andExpect(status().isOk())                .andExpect(content().string(employee.toString()));        verify(employeeServiceImpl, times(1)).publishEmployee(any(Employee.class));    }    @Test    void testPostEmployeeRecordWithNoBody() throws Exception {        mockMvc.perform(post("/employee/create")                        .contentType(MediaType.APPLICATION_JSON))                .andExpect(status().isBadRequest());    }    @Test    void testGetEmployeeRecord() throws Exception {        // Controller method is not implemented yet        mockMvc.perform(get("/employee/get")                        .param("id", String.valueOf(getEmployeeRecord().getId())))                .andExpect(status().isOk())                .andExpect(content().string("")); // TODO Since method returns null    }    private Employee getEmployeeRecord() {        Employee employee = new Employee();        employee.setId(1);        employee.setFirstName("Dallas");        employee.setLastName("Texes");        employee.setAge(10);        employee.setLocation("USA");        return employee;    }}